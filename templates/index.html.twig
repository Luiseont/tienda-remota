{% extends "layouts/default.html.twig "%}

{% block content %}


<div class="container">
    <div class="row">
        {% for shirt in shirts %}
        <div class="col-md-3">
          <div class="card mb-4 shadow-sm">
            <img class="card-img-top" src={{"../Static/img/"~ shirt.img}} height="270" alt="Card image cap">
            <div class="card-body">
                <p><strong>Precio: </strong>{{shirt.precio ~ " Є"}}</p>
                <p><strong>Talla: </strong>{{shirt.talla}}</p>
                <p><strong>Color: </strong>{{shirt.color}}</p>
            </div>
          </div>
        </div>
        {% endfor %}
     </div>
</div>

{% endblock  %}